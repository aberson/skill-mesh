Completion gate: all six implementation/observation steps remain PLANNED; full fresh-context check applies.

# Complete build workflows: publication 2 fresh-context readiness

Target: `documentation/codex-deep-review-restoration-plan.md`, publication 2.
This follows the publication-2 technical review and the refreshed stable HTML
proposal. It supersedes the narrow publication-1 readiness result for execution.
READY describes the plan instructions, not implemented support or host acceptance.

- §1 Schemas and data structures — pass: lens/finding/invocation/sidecar records, support configuration and local receipts are defined; checkpoint schema and helper are pinned explicitly during bootstrap.
- §2 Identifiers — pass: immutable commit IDs, SHA-256, review UUIDs, native session IDs, prior sidecars and runtime candidate/build identities are specified.
- §3 Acronyms and tool names — pass: ordinary full versus code-deep, source/profile/consumer roots, calibration, parent authority and W1-W6 are explained in context.
- §4 Stack decisions with rationale — pass: existing Python/PowerShell/Bash plus an explicitly checked Playwright/Chromium dependency; the fixture is a small standard-library HTTP app.
- §5 Unresolved decisions — pass: P1-P3 and D1-D10 have stable IDs and selected defaults. D1 remains an agent default, not an attributed operator packaging choice. Missing capabilities have concrete stop behavior.
- §6 API contracts — pass: fixture health, UI/asset, quote and order endpoints have request/result shapes and intended rules; aggregate invocation JSON and resume locator are specified.
- §7 Development process — pass: deep bootstrap expires after Step 121; checkpoint dependency bootstrap expires after Step 122; exact-candidate review/gates precede landing, and a common certified release precedes both final host runs.
- §8 Quickstart / how to run — pass: prerequisites, explicit source variables, build/test commands, numbered range and finite overnight contract are provided. Step 123 must produce exact native start/resume commands before observation.
- §9 Referenced external files — pass: existing producer paths were inspected, new outputs are labeled new, and protected worktrees remain separate. No developer-local absolute paths or secret service state are committed.
- §10 Scope and constraints — pass: actual ordinary-full and code-deep workflow on both hosts; installed-only operation, deep runtime/full modes and the retired certification/installer programs remain outside the stated support claim.
- §11 Operator/code step-shape integrity — pass: Steps 120-123 author code and procedure; 124-125 execute prepared native observations. The fixture's separate wait transition retains pending state until the new coordinator observes completion.
- §12 Conditional steps declare a Condition predicate — N/A: there are no conditional implementation steps; host prerequisites never waive a mandatory acceptance row.
- §13 Substrate-smoke step present — pass: a short real app/capture/checkpoint rehearsal precedes two actual installed-host W1-W6 runs with genuine fresh coordinator boundaries.

## Blocker

None in the execution instructions. The prerequisite repair's full gate/installation
and native launcher qualification remain pending. Claude availability and handoff
reconciliation remain prerequisites for the common release and final acceptance.
These conditions prevent dispatch or completion as specified; this report does not
override them.

## Gap

None within the bounded objective. Both hosts must pass every W1-W6 row on the
same release and fixture. Component tests, calibration replay, emitted text and
the presence of a dispatch tool are insufficient completion evidence.

## Minor

Issue placeholders are intentionally awaiting the immediately following repo-sync.
Backfilling tracking IDs changes no implementation or acceptance requirement.
The stable HTML marks D1 (support dependency), D4 (expiring bootstrap) and D8
(finite overnight work) as the principal defaults to inspect.

READY
