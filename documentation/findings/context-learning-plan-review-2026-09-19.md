Reviewing as: feature plan. Sections 17–21 apply.

# Publication 2 technical review — 2026-09-19

Target: [revised portfolio](../context-learning-plan.md). Applied plan-review in the parent session after incorporating the earlier independent adversarial scope review. This is a documentation review, not a new independent review or runtime certification. No populated Issue fields; no issue synchronization.

## Blockers

**B1 — portfolio is not a build-phase input (§25).** The document uses Project headings, not numbered Step headings with Problem/Type/Issue fields; section 5 explicitly says “Dispatch build-phase on that prepared slice, never this portfolio.” This is intentional for scope review, but remains a blocker to direct implementation/issue synchronization from this file. Prepare only Project 2's repository-scoped implementation and separate attended acceptance units, with actual number reconciliation. Do not recreate the abandoned seven-step chain to satisfy the parser.

## Significant gaps

**G1 — capture's exact invocation/storage interface belongs to slice preparation (§§1, 6, 8, 10, 16).** Project 2 says its executable plan will freeze “the marker literal, capture command and ignored private storage directory.” Record fields and failure semantics are present, but a fresh builder still lacks the command arguments, bounds, distribution location and concrete setup/first-run procedure. Resolve these before building Project 2, using a supported helper asset path or an explicitly scoped resource plan. This does not require the deferred general runtime package.

**G2 — trial route is unqualified (§§2, 15, 17).** Project 3 correctly reports no Codex workflow mapping. Its half-day timebox is for investigation; it is not an implementation estimate for an adapter. Qualification must yield observed independent scoring, or an unavailable result and separate scope. It does not block Project 2.

## Missing items

None beyond B1/G1's explicitly pending executable-slice artifacts. Planned pyproject/lock/helper outputs are not described as existing files.

## Nice-to-haves

None. Do not add dashboards, fixed observation quotas or broad test campaigns while closing B1/G1.

## Evidence and scope checks

| Plan claim | Read-only evidence | Consequence |
|---|---|---|
| Resume already has an owner | M1 plan lines 172–177 and 214–223: explicit prior-session selection, retained default compatibility, canonical helper and caller proof | Reuse M1; do not infer global default-hook safety |
| Default fallback still exists | coding-root .claude/hooks/lib/task-state-derive.ps1 lines 153–171: own path, then freshest session, then rollup | A residual fix is separate and evidence-driven |
| Harvest skips new non-Git observations today | skills/lesson-harvest/core.md line 48 stops at equal HEAD; lines 100–110 own five-store dedup; lines 170/190–191 preserve read-only dry-run | New observation progress is independent; preserve final dry-run contract |
| Evaluator route is not established | skills/skill-evolve/providers/codex.md line 9 explicitly maps no workflow primitive and prohibits producer self-grading | Qualification is real work; generic isolated agents do not prove it |
| CL does not implement the complete evaluator | documentation/skill-catalog-lifecycle-plan.md lines 271–272 exclude retargeting; Step 114 begins at 470 and produces legacy guard/redirect | Separate narrow follow-up if qualification needs canonical retargeting |
| AP should remain small | documentation/user-afterparty-effectiveness-plan.md lines 45–47/65: mandatory harvest dry-run, no child/helper/builder/installer expansion | Add saved-result reference later; no AP blocker for capture |
| Observatory can reuse a reader | dev-observatory/src/dev_observatory/view_sources.py lines 273–325: summary keys and bounded rows | Producer artifact plus later registry wiring; no new engine |
| Required gates remain | Skill Mesh CLAUDE.md: repo-root python -m pytest, all provider builds, no lint/typecheck | No reduction of controlling gates |

Local main is a339620; git worktree inventory still includes M1, coordinator-handoff and baseline/release work. Those trees were not modified. Root plan edit changes only this proposal's pointer.

Checklist coverage: §§1–16 cover private immutable observations, partial-write/idempotency policy, single applying harvester, evidence handling, explicit rather than scheduled capture, bounded isolated trials, required gates and one observed cycle. §§17–21 checked the concrete existing owners above and limited changes to this follow-up. §§22–24/27 have no executable steps to route yet; §25 yields B1. §26's real-host acceptance obligation is carried into each future runtime slice, not claimed completed.

## Adversarial findings incorporated

The prior independent reviewer /root/adversarial_scope_review found journal-first coupling, arbitrary observation quotas, the blanket 24-execution matrix, excessive default deep review, and an unproved Codex evaluator route. Publication 2 removes those prerequisites/defaults while preserving independent grading, source ownership, repository gates and changed-hook smoke. The user accepted this direction before this revision.

Auto-applied 0 mechanical fixes. Scope changes were explicitly requested. Before build: resolve B1/G1 in the selected slice; resolve G2 only when preparing Project 3. No additional user confirmation is needed to finish this publication.
