Completion gate: no consistent completion markers found -- running full check (fail-safe default).

# Publication 2 fresh-context check — 2026-09-19

Target: [revised plan](../context-learning-plan.md), after [technical review](context-learning-plan-review-2026-09-19.md) and [redline publication 2](../context-learning-proposal.html). Applied plan-wrap in the parent session. No implementation units are declared complete.

This is a reviewable portfolio with a deliberately pending executable-slice handoff. The full forward checklist still applies; the verdict below is about ability to build directly from this document.

§1 Schemas and data structures — findings: 1. Observation fields are summarized; concrete disposition/capture wire details remain in G1.

§2 Identifiers — pass. Observation UUID version/generator/retry identity and correction references are defined; proposal IDs remain stable. Future optional summary IDs must follow the existing reader's contract when that slice is scoped.

§3 Acronyms and tool names — pass. M1, AP, CL and uv are explained; named skills link to their owning contracts.

§4 Stack decisions with rationale — pass. Reuse Markdown and existing owners; uv is independent development maintenance, not a new runtime architecture.

§5 Unresolved decisions — findings: 1. G2 tracks actual evaluator capability/route; a budget timebox is not capability evidence. D9 is an explicit queue default, not a hidden dependency.

§6 API contracts — N/A: no backend routes are introduced; helper invocation is tracked in G1.

§7 Development process — findings: 1. B1: project cards cannot dispatch a build. Required gates, canonical ownership and separate attended acceptance are specified for slice preparation.

§8 Quickstart / how to run — findings: 1. G1: exact capture setup/command/path must be frozen in the selected executable plan.

§9 Referenced external files — pass. Local Markdown/HTML links were checked; planned helper/pyproject/lock assets are declared outputs, not missing existing files. Generic skills/name paths describe topology, not a claimed file.

§10 Scope and constraints — pass. First estimate is capture-only; deferred work, private evidence, review-before-promotion and existing build authority are explicit.

§11 Operator/code step-shape integrity (Blocker if violated) — N/A: no executable step blocks; preparation explicitly separates code/runbook creation from attended acceptance.

§12 Conditional steps must declare a Condition: predicate (Blocker) — N/A: optional portfolio projects are not conditional build-step declarations.

§13 Substrate-smoke step present when the plan touches deployment seams (Significant Gap) — findings: 1. B1 includes translating the specified real-host observation into an executable attended step; no runtime acceptance is claimed.

## Blocker

**B1 — §7/§13: no executable build units.**
Found at: “Project cards are not executable step reservations” and “Dispatch build-phase on that prepared slice, never this portfolio” (section 5).
Fix: prepare Project 2's narrow repository-owned plan, with numbered implementation and separate attended installed-host acceptance steps, actual files/flags/commands and acceptance procedure. Reconcile number reservations before issue synchronization. This is the next preparation action, not a reason to widen the current portfolio.

## Gap

**G1 — §1/§8: exact capture and progress contract not yet frozen.**
Found at: “freeze the marker literal, capture command and ignored private storage directory before build” (Project 2).
Fix: define command arguments/bounds, distribution location, observation/disposition storage and concrete first-run commands in the selected slice. Preserve the specified immutable observations, idempotency, single applying harvester and read-only preview.

**G2 — §5: evaluator route needs qualification.**
Found at: “First prove one actual route can run the scoring workflow” (Project 3).
Fix: record a real independently graded cycle or an unavailable outcome. If an adapter or canonical scorer change is needed, prepare and estimate that bounded follow-up. This is a Project 3 dependency, not a Project 2 blocker.

## Minor

None.

## Publication verification

Final verification passed: 66 local links across nine Markdown files and the proposal, balanced HTML, matching 14 decision IDs (P1–P4 and D1–D10), no external assets, and no trailing whitespace. The initially pending wrap-report link now resolves. Earlier IDs were not removed or renumbered. The proposal retains local light/dark styling and print rules. The root plan pointer also passes git diff --check. This checks document structure, not browser rendering or runtime behavior.

No code tests, installed-host probes, full repository gate, issue mutations, commits or pushes were performed for this documentation-only revision.

## Needs your input

No new answer is required to complete the requested publication. D9 (queue), D4 (trial bounds) and D6 (collection automation) remain visible adjustment points. The build-readiness findings above are preparation work, not a request to approve an unseen implementation.

Next: review publication 2; prepare only the chosen capture/harvest executable slice when moving toward build.

NEEDS WORK: 1 blockers, 2 gaps
