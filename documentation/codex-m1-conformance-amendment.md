# M1 workflow-conformance amendment

Date: 2026-09-10. Decision P5 follows the operator's clarification that the tools
should follow the same steps while different LLMs may produce different valid
results, followed by explicit authorization to update the plans.

This record explains the amendment. The executable contract remains
[codex-ordinary-build-milestone-plan.md](codex-ordinary-build-milestone-plan.md);
[plan.md](../plan.md) alone owns mutable execution status and evidence locators.
The original HTML proposal and September 9 approval/review artifacts remain
unchanged history.

## Changes

| Before | After | Decision owner |
|---|---|---|
| M1 required an independent reviewer to discover each prescribed planted defect; a miss failed acceptance. | M1 proves workflow conformance and valid functional outcomes. Particular findings, matching finding counts and equal model outputs are not completion thresholds. | P5: operator |
| Incomplete fixture tests stayed green so review had to catch the bug. | Boundary and ownership tests catch the fixed invalid candidates through the real mechanical gate. A fresh developer fixes them; unchanged tests pass before actual full reviews run. | D13: agent-selected method implementing P5 |
| Live review rejection was required even if no natural blocking finding occurred. | Record actual review outcomes. Honor every real blocking finding; deterministic caller tests cover blocking-verdict routing without impersonating a native review. | D13: agent-selected method |
| Current execution text still described a failed early qualification. | The status index records the later qualification/publication and active Step 126 candidate gate, with dated evidence. | D14: evidence reconciliation |

Steps 126/127 still build the required runtime/caller and checkpoint/resume
support. Steps 128/129 retain the real installed Codex exercise: two fixture code
steps, ordinary five-code/three-runtime independent reviews, a wait and an actual
native coordinator transition. No exact cross-model implementation or judgment
comparison is added. This is not a reduction to mocked or source-only acceptance.

Actual correctness remains required. Known test failures, missing evidence,
invalid reviewer receipts, unresolved blocking findings and skipped required
steps still block advancement. A mechanical failure is labeled as such; it is
never presented as proof that a reviewer found a bug. Reviewer quality observations
remain evidence-backed and do not require a separate campaign before M1 completes.

The full-root test obligations, model pins, independence, parent-only advancement
authority, support/candidate identities and finite execution limits are retained.
Exact mechanical identities protect evidence integrity; they do not demand equal
LLM outputs. Test-frequency optimization is separate from this amendment.

Deep review and Claude acceptance remain deferred. When refreshed, their plans
must apply P5 too; the historical stronger detection threshold is not a default
release condition. Existing deterministic calibration tests are not rewritten.
M1 completion still does not itself resume a parked consumer product build.

## Integration and execution boundary

The amendment is authored on `plan/m1-workflow-conformance-20260910`, based on
`b8acf82924cc3166a078cc50caa26b08d1135321`, while the existing coordinator has a
pinned candidate gate in flight. This branch may be committed, published and
reviewed, and its planning issues synchronized with a pending-integration notice.
Do not move main or change any frozen controller, candidate, manifest or receipt.
Update the changed planning bodies #196/#200/#201 now. Preserve active #199 and
unchanged #204 until the adoption handoff, which refreshes their plan links to
the integrated commit without changing either step's acceptance criteria.

After the active controller ends, reconcile the current checkpoint, terminal
evidence and Git before integrating the docs amendment. Preserve any detached
candidate gate until it finishes. Record the integrated plan commit and prior
run disposition in the next handoff before dispatch; its new coordinator must
qualify its own native authority. The current run retains its original deadline
and consumed iteration count. This amendment grants no extra attempt, fourth
developer iteration, automatic relaunch or retrospective PASS.

## Validation

- [Technical plan review](findings/codex-m1-conformance-plan-review-2026-09-10.md)
  checks source contracts, scope, gate ordering, issue coverage and live-run safety.
- [Fresh-context plan check](findings/codex-m1-conformance-plan-wrap-2026-09-10.md)
  checks step structure, support/evidence shapes and the adoption boundary.
- Documentation changes receive reference, diff and structural checks. No source,
  installed skill, fixture or test is changed, and no implementation step is marked
  DONE by this publication.
